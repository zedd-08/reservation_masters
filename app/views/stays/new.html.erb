<div class="mx-auto md:w-2/3 w-full">
  <h1 class="font-bold text-4xl">New stay</h1>

  <%= render "form", stay: @stay %>
  <br/>
  <%= link_to "Back to stays", stays_path, class: "btn btn-secondary" %>
</div>
